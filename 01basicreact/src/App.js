import Chai from "./chai";
function App() {
  return (
    <>
      <h1>
        This is my Ultimate react learning. After this i will confident in
        react. creation of react app using npx create-react-app is quit slower,
        that vite , but it install dependecies together , no need to do npm
        install extra , js do npm run stat , to run our app NEED : when we make
        app with npx create-react-app , make sure to make function name with
        capital, no need to jsx, .js will also work
      </h1>
      <Chai></Chai>
    </>
  );
}

export default App;
